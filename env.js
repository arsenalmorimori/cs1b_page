const url = window.env.URL;
const key = window.env.KEY;
const receipt_webhook = window.env.RECEIPT_WEBHOOK;
const announece_webhook = window.env.ANNOUNCE_WEBHOOK;
const savings_webhook = window.env.SAVINGS_WEBHOOK;
